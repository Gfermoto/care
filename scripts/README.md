# Скрипты для работы с радаром LD2450

Утилиты для предварительной настройки и мониторинга радара через USB-UART.

## 📁 Файлы

### `connect_radar.sh` - Подключение радара в WSL

**Назначение:** Автоматическое подключение радара к WSL через `usbipd`

**Использование:**
```bash
./scripts/connect_radar.sh [BUSID]
RADAR_BUSID=3-4 ./scripts/connect_radar.sh   # через переменную окружения
```

**Что делает:**
- Привязывает USB устройство через `usbipd-win`
- Подключает радар к WSL2
- Проверяет доступность порта `/dev/ttyUSB0` или `/dev/ttyUSB1`
- Показывает команды для работы с радаром (с учётом найденного порта)

**Требования:**
- Windows с установленным `usbipd-win`
- WSL2
- Радар подключен к USB порту Windows

---

### `radar.py` - Мониторинг данных радара

**Назначение:** Мониторинг данных радара в реальном времени с правильным парсингом протокола LD2450

**Использование:**
```bash
python3 scripts/radar.py                    # порт по умолчанию /dev/ttyUSB0
python3 scripts/radar.py -p /dev/ttyUSB1   # указать порт
python3 scripts/radar.py -b 115200          # указать скорость
python3 scripts/radar.py --help             # все опции
```

**Что делает:**
- Подключается к указанному порту (по умолчанию `/dev/ttyUSB0`) на **256000 baud**
- Ищет заголовок пакетов **`AA FF`** (протокол LD2450)
- Парсит координаты X, Y (signed 16-bit) из пакетов
- Выводит координаты целей в миллиметрах
- Считает количество обработанных целей

**Пример вывода:**
```
📡 Радар HLK-LD2450
Скорость: 256000 бод
Протокол: AA FF
==================================================
Пробуем 230400 бод... ❌
Пробуем 250000 бод... ❌
Пробуем 256000 бод... ✅
✅ Подключено на 256000 бод
Ищем данные радара (AA FF)...

🎯 x: 1234 мм, y: 5678 мм
🎯 x: 1235 мм, y: 5679 мм
--- Обработано 20 целей ---
```

**Требования:**
- Python 3
- Библиотека `pyserial`: `pip install -r scripts/requirements.txt` или `pip install pyserial`

**Особенности:**
- Автоматически пробует разные скорости (230400, 250000, 256000, 115200)
- Правильный парсинг протокола LD2450
- Корректная обработка знаков координат
- Вывод координат в миллиметрах

---

## 🔧 Установка зависимостей

```bash
# Python библиотеки для работы с UART
pip install -r scripts/requirements.txt
# или: pip install pyserial
```

---

## 📋 Быстрый старт

### 1. Подключение радара (WSL)

```bash
./scripts/connect_radar.sh
```

### 2. Мониторинг данных

```bash
python3 scripts/radar.py
```

---

## 🔍 Устранение проблем

**Порт не найден:**
- Проверьте подключение радара к USB порту
- Запустите `connect_radar.sh` для WSL
- Проверьте порты: `ls -la /dev/ttyUSB*`

**Данные не приходят:**
- Убедитесь что радар включен и питается от 5V
- Проверьте скорость UART - должно быть **256000 baud**
- Проверьте подключение TX/RX (перекрёстное)

**Ошибка "Permission denied":**
```bash
sudo chmod 666 /dev/ttyUSB0
# Или добавьте пользователя в группу dialout
sudo usermod -a -G dialout $USER
```

---

## 📚 Дополнительно

См. `GUIDE_ESPHome_HASSio_UART.md` в корне репозитория для полной документации по USB-UART настройке радара.

