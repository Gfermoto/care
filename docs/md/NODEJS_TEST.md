# 🧪 Тест Node.js компонентов C.A.R.E.

## 📋 Проверка Node.js сборки

### **Что мы проверим:**

1. ✅ **Установка Node.js** и npm
2. ✅ **Установка зависимостей** из package.json
3. ✅ **Проверка синтаксиса** всех .js файлов
4. ✅ **Тест импортов** модулей
5. ✅ **Проверка зависимостей** библиотек

### **Проблемы, которые могут возникнуть:**

#### **1. Node.js не установлен:**
```bash
# Установка Node.js 18.x
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

#### **2. Зависимости не устанавливаются:**
```bash
# Очистка кэша npm
npm cache clean --force

# Установка с правами sudo (если нужно)
sudo npm install
```

#### **3. Проблемы с нативными модулями:**
```bash
# Установка build-essential
sudo apt-get install -y build-essential

# Переустановка нативных модулей
npm rebuild
```

#### **4. Проблемы с ROS 2:**
```bash
# rclnodejs требует ROS 2
# Если ROS 2 не установлен, CAN Bridge не будет работать
# Но Dashboard и API будут работать
```

## 🚀 **Ручной тест сборки:**

### **1. Проверка Node.js:**
```bash
cd /home/gfer/CARE/nodejs
node --version
npm --version
```

### **2. Установка зависимостей:**
```bash
npm install
```

### **3. Проверка синтаксиса:**
```bash
node -c index.js
node -c care-can-bridge/index.js
node -c care-dashboard/index.js
node -c care-api/index.js
```

### **4. Тест импортов:**
```bash
# Тест CAN Bridge
node -e "const CareCANBridge = require('./care-can-bridge/index.js'); console.log('CAN Bridge OK');"

# Тест Dashboard
node -e "const CareDashboard = require('./care-dashboard/index.js'); console.log('Dashboard OK');"

# Тест API
node -e "const CareAPI = require('./care-api/index.js'); console.log('API OK');"
```

## 🔧 **Возможные проблемы и решения:**

### **Проблема 1: socketcan не устанавливается**
```bash
# На Ubuntu/Debian
sudo apt-get install libsocketcan-dev

# Переустановка
npm install socketcan --build-from-source
```

### **Проблема 2: rclnodejs не работает**
```bash
# Требует ROS 2
# Если ROS 2 не установлен, CAN Bridge не будет работать
# Можно запустить только Dashboard и API
```

### **Проблема 3: WebSocket проблемы**
```bash
# Проверка портов
netstat -tlnp | grep :3000
netstat -tlnp | grep :3001

# Освобождение портов
sudo kill -9 <PID>
```

## 📊 **Ожидаемые результаты:**

### **✅ Успешная сборка:**
```
[SUCCESS] Node.js найден: v18.17.0
[SUCCESS] npm найден: 9.6.7
[SUCCESS] Зависимости установлены
[SUCCESS] Синтаксис OK
[SUCCESS] Импорты OK
```

### **⚠️ Частичная сборка:**
```
[SUCCESS] Node.js найден: v18.17.0
[SUCCESS] npm найден: 9.6.7
[SUCCESS] Зависимости установлены
[SUCCESS] Синтаксис OK
[WARNING] CAN Bridge - требует ROS 2
[SUCCESS] Dashboard - импорт OK
[SUCCESS] API - импорт OK
```

### **❌ Ошибка сборки:**
```
[ERROR] Node.js не установлен
[ERROR] Зависимости не установлены
[ERROR] Синтаксис ошибка
```

## 🎯 **Запуск после сборки:**

### **Все сервисы:**
```bash
cd nodejs
npm start
```

### **Отдельные сервисы:**
```bash
# Только Dashboard (без ROS 2)
npm run dashboard

# Только API
npm run api

# CAN Bridge (требует ROS 2)
npm run bridge
```

## 🔍 **Проверка работоспособности:**

### **1. Dashboard:**
- Откройте http://localhost:3000
- Должен появиться интерфейс C.A.R.E.

### **2. API:**
```bash
curl http://localhost:3001/health
```

### **3. CAN Bridge:**
```bash
# Требует ROS 2 и CAN интерфейс
ros2 topic list
```

## 🎉 **Итог:**

**Node.js компоненты C.A.R.E. должны собираться без проблем**, если:

- ✅ **Node.js 18+** установлен
- ✅ **npm** работает
- ✅ **build-essential** установлен
- ✅ **Интернет** доступен для загрузки зависимостей

**Возможные ограничения:**
- ⚠️ **CAN Bridge** требует ROS 2
- ⚠️ **socketcan** может требовать системные библиотеки
- ⚠️ **Порты 3000/3001** должны быть свободны
