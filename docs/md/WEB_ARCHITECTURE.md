# 🌐 C.A.R.E. - Архитектура веб-интерфейсов

## 📋 Обзор

C.A.R.E. использует разные подходы для веб-интерфейсов в зависимости от платформы микроконтроллера:

- **ESP32**: Встроенный веб-сервер на микроконтроллере
- **STM32**: Веб-интерфейс через Node.js на хосте

## 🤖 ESP32 - Встроенный веб-сервер

### Характеристики
- **Платформа**: ESP32 с FreeRTOS
- **Веб-сервер**: AsyncWebServer (встроенный)
- **Файловая система**: LittleFS
- **WiFi**: Конфигурация и веб-интерфейс
- **CAN**: Встроенный TWAI интерфейс

### Архитектура
```
ESP32
├── 📡 LD2450 Radar (UART)
├── 🌐 WiFi AP/STA
├── 📁 LittleFS (веб-файлы)
├── 🔌 CAN TWAI (ROS 2)
└── 🌐 AsyncWebServer
    ├── / (главная страница)
    ├── /config (настройки)
    ├── /api/targets (данные целей)
    └── /api/status (статус системы)
```

### Преимущества
- ✅ **Автономность**: работает без внешнего компьютера
- ✅ **Производительность**: минимальная задержка
- ✅ **Простота**: один микроконтроллер для всего
- ✅ **Надежность**: меньше точек отказа

### Недостатки
- ❌ **Ограниченная функциональность**: простой интерфейс
- ❌ **Ресурсы**: ограниченная память ESP32
- ❌ **Сложность разработки**: ограниченные возможности

## 🔧 STM32 - Node.js веб-интерфейс

### Характеристики
- **Платформа**: STM32F407 + хост-компьютер
- **Веб-сервер**: Node.js Express
- **CAN**: Внешний CAN-трансивер
- **ROS 2**: На хосте или в Docker

### Архитектура
```
STM32F407          Host Computer
├── 📡 LD2450 ────┐
├── 🔌 CAN ───────┼─── Node.js Services
└── 📊 Data ──────┘    ├── 🌐 Web Dashboard
                       ├── 🔌 REST API
                       ├── 📡 CAN Bridge
                       └── 🤖 ROS 2 Integration
```

### Преимущества
- ✅ **Мощный интерфейс**: полные возможности веб-технологий
- ✅ **Легкая разработка**: Node.js экосистема
- ✅ **Расширяемость**: легко добавлять функции
- ✅ **Мониторинг**: продвинутая аналитика

### Недостатки
- ❌ **Зависимость**: требует хост-компьютер
- ❌ **Сложность**: больше компонентов
- ❌ **Задержка**: сетевые задержки

## 🔄 Выбор платформы

### Используйте ESP32 когда:
- Нужна автономная система
- Простой веб-интерфейс достаточен
- Важна надежность
- Ограничен бюджет

### Используйте STM32 + Node.js когда:
- Нужен мощный веб-интерфейс
- Требуется интеграция с ROS 2
- Важна расширяемость
- Есть хост-компьютер

## 🚀 Реализация

### ESP32 веб-интерфейс
```cpp
// Встроенный веб-сервер
AsyncWebServer server(80);

// Статические файлы из LittleFS
server.serveStatic("/", LittleFS, "/web/");

// API эндпоинты
server.on("/api/targets", HTTP_GET, handleTargets);
server.on("/api/config", HTTP_POST, handleConfig);
```

### STM32 + Node.js веб-интерфейс
```javascript
// Express сервер
const express = require('express');
const app = express();

// Веб-интерфейс
app.use(express.static('public'));

// API эндпоинты
app.get('/api/targets', getTargets);
app.post('/api/config', updateConfig);
```

## 📊 Сравнение

| Характеристика | ESP32 | STM32 + Node.js |
|----------------|-------|-----------------|
| Автономность | ✅ Высокая | ❌ Требует хост |
| Функциональность | ⚠️ Ограниченная | ✅ Полная |
| Производительность | ✅ Высокая | ⚠️ Зависит от сети |
| Разработка | ❌ Сложная | ✅ Простая |
| Надежность | ✅ Высокая | ⚠️ Больше компонентов |
| Стоимость | ✅ Низкая | ⚠️ Выше |

## 🎯 Рекомендации

1. **Для простых систем**: ESP32 с встроенным веб-интерфейсом
2. **Для сложных систем**: STM32 + Node.js
3. **Для разработки**: STM32 + Node.js (легче тестировать)
4. **Для продакшена**: ESP32 (надежнее)

## 🔮 Будущее

В будущем планируется:
- **STM32**: Перенос веб-интерфейса в микроконтроллер
- **ESP32**: Улучшение веб-интерфейса
- **Унификация**: Общий API для обеих платформ

