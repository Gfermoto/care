# C.A.R.E. Demo Guide - Запуск с Mock датчиком и RViz2

## 🎯 Обзор
Этот гайд поможет запустить полную демонстрацию C.A.R.E. системы с Mock датчиком и красивой 3D визуализацией в RViz2.

## ✅ Что уже готово
- ✅ ROS 2 Jazzy установлен и настроен
- ✅ RViz2 готов к работе
- ✅ Mock датчик настроен
- ✅ ROS2 пакет `care_radar_publisher` собран
- ✅ Красивая конфигурация RViz2 создана

## 🚀 Быстрый запуск

### 1. Запуск полной системы
```bash
cd /home/gfer/CARE
./scripts/launch_care_demo.sh
```

Этот скрипт автоматически:
- Загружает ROS2 окружение
- Запускает ROS2 ноду с Mock данными
- Открывает RViz2 с красивой конфигурацией

### 2. Мониторинг системы (в отдельном терминале)
```bash
cd /home/gfer/CARE
./scripts/monitor_care.sh
```

## 🎨 Что вы увидите в RViz2

### Визуализация включает:
- 🎯 **Цели радара** - цветные сферы (красные = опасные, желтые = предупреждение, зеленые = безопасные)
- 🛡️ **Зона безопасности** - красная полупрозрачная область
- 🔄 **TF фреймы** - система координат (map → base_link → radar_link)
- 📊 **Сетка** - для ориентации в пространстве

### Цветовое кодирование:
- 🔴 **Красные цели**: В зоне опасности (< 1.5м, в пределах ±60°)
- 🟡 **Желтые цели**: Предупреждение (1.5-2.5м)
- 🟢 **Зеленые цели**: Безопасные (> 2.5м)

## 📊 Топики ROS2

### Активные топики:
- `/care/radar_targets` - MarkerArray с целями
- `/care/safety_zone` - Marker зоны безопасности  
- `/care/status` - String статус системы
- `/tf` - Трансформации координат

### Мониторинг топиков:
```bash
# Просмотр всех топиков
ros2 topic list | grep care

# Статус системы
ros2 topic echo /care/status

# Данные целей
ros2 topic echo /care/radar_targets

# Информация о топике
ros2 topic info /care/radar_targets
```

## 🎮 Управление RViz2

### Навигация:
- **Левая кнопка мыши** + перетаскивание = поворот камеры
- **Правая кнопка мыши** + перетаскивание = панорамирование
- **Колесо мыши** = приближение/отдаление
- **Средняя кнопка мыши** = фокус на точке

### Полезные настройки:
- **Fixed Frame**: `map` (основная система координат)
- **Target Frame**: `base_link` (следование за роботом)
- **Grid**: Включена для ориентации

## 🔧 Настройка и кастомизация

### Изменение параметров Mock датчика:
Отредактируйте файл `services/ros2/care_radar_publisher/src/care_radar_node.cpp`:
- `safety_distance = 1.5` - дистанция безопасности (метры)
- `safety_angle = M_PI / 3` - угол безопасности (±60°)
- Частота обновления в таймерах

### Изменение внешнего вида в RViz2:
1. Откройте RViz2
2. Измените настройки дисплеев
3. Сохраните: File → Save Config As...

## 🛠️ Устранение неполадок

### RViz2 не запускается:
```bash
# Проверка дисплея
echo $DISPLAY

# Проверка ROS2
source /opt/ros/jazzy/setup.bash
rviz2 --help
```

### Нет данных в топиках:
```bash
# Проверка ноды
ros2 node list | grep care

# Перезапуск ноды
pkill -f care_radar_node
ros2 run care_radar_publisher care_radar_node
```

### Проблемы с TF:
```bash
# Просмотр TF дерева
ros2 run tf2_tools view_frames.py

# Эхо трансформаций
ros2 run tf2_ros tf2_echo map base_link
```

## 📈 Расширение системы

### Добавление реального датчика:
1. Замените Mock данные на реальные от LD2450
2. Настройте CAN интерфейс
3. Подключите ESP32/STM32

### Интеграция с роботом:
1. Добавьте URDF модель робота
2. Настройте навигацию
3. Подключите планировщик траекторий

## 🎯 Следующие шаги
- Тестирование с реальным LD2450 радаром
- Интеграция с ESP32/STM32
- Добавление веб-интерфейса Node.js
- Развертывание в Docker

## 📞 Поддержка
При возникновении проблем проверьте:
1. ROS2 окружение загружено
2. Все процессы запущены
3. Топики публикуют данные
4. RViz2 подключен к правильным топикам

