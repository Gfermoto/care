# 🧭 C.A.R.E. Целевая архитектура (изящная и модульная)

## 1) Контроллер (ESP/STM)
- Прозрачно ретранслирует ВСЕ сообщения радара в CAN: `0x200-0x202`
- Генерирует только одно системное сообщение: `0x100` (Emergency Stop)
- Периодически шлёт `0x300` (System Status)
- UART: выводит инженерные/дебаг сообщения

## 2) Настройка через CAN
- Конфигурационные команды в CAN:
  - `0x400` → Настройки контроллера (битрейт CAN, режимы, и т.д.)
  - `0x401` → Настройки радара (частота обновления, чувствительность, дальность)
- Ответы/ACK:
  - `0x480` → ACK/ERROR на конфиг контроллера
  - `0x481` → ACK/ERROR на конфиг радара

## 3) UART инженерный канал
- Отдельный низкоуровневый скрипт для настройки/диагностики через UART
- Поддерживает чтение статуса, изменение параметров, сброс, сохранение

## 4) Вне ROS компоненты (отдельные сервисы)
- ESP: Web UI (настройка/демо)
- STM+ESP: Node.js сервис (настройка/демо, COM, WebSocket)
- Эти сервисы не зависят от ROS и будут контейнеризованы позже

## 5) ROS2 – три независимые ноды
- Bridge Node: прозрачная передача целей + приём `0x100` (emergency)
- Demo Node: демонстрация (RViz) + настройка контроллера/радара
- Safety Node: продвинутая логика безопасности (реакция/алгоритмы)

Все ноды поддерживают режимы `real` и `mock`.

---

## Связь с текущей реализацией
- CAN IDs: 0x100, 0x200-0x202, 0x300 — уже реализовано
- Добавляются конфиг-IDs: 0x400/0x401 (+ ACK 0x480/0x481)
- Документация и инструменты:
  - `docs/CAN_MESSAGES.md` — будет дополнен секцией 0x400/0x401
  - `tools/debug_com_port.py` — инженерный UART уже добавлен
  - `services/nodejs/stm32-config/` — отдельный Node.js сервис

## Режимы mock/real
- Переключение через параметр запуска ноды: `--ros-args -p mode:=mock|real`
- В mock режиме генерируются синтетические цели и статусы
- В real режиме используется CAN-шина/COM/UART
