# 🛡️ C.A.R.E. - Collision Avoidance Radar Emergency System

Высокопроизводительная система предотвращения столкновений на базе радара LD2450 с интеграцией ROS 2 и веб-интерфейсом.

## 📁 Структура проекта (актуально)

```
CARE/
├── 🤖 platforms/                 # Платформы микроконтроллеров
│   ├── esp32/                   # ESP32 автономная система
│   └── stm32/                   # STM32F407 с встроенным CAN
├── 🌐 services/                 # Сервисы хоста
│   ├── nodejs/                  # Node.js сервисы (вне ROS)
│   │   └── stm32-config/        # Настройка STM/ESP через COM + WebSocket
│   └── ros2/                    # ROS2 пакеты (минимальный набор)
│       ├── care_common/         # Общие сообщения/сервисы
│       ├── care_can_bridge_node/# CAN ↔ ROS мост (mock/real, typed+raw)
│       ├── care_demo_node/      # Демо (RViz) + конфиг через CAN (0x400/0x401)
│       └── care_safety_controller_node/ # Логика безопасности (высокоуровневая)
├── 🔧 infrastructure/           # Скрипты и развёртывание
│   └── scripts/                 # Конфигурация/установка/запуск
├── 📚 docs/                     # Документация
│   ├── ARCHITECTURE_TARGET.md   # Целевая архитектура
│   ├── CAN_MESSAGES.md          # CAN протокол (с конфиг/ACK)
│   └── CONTROLLER_RADAR_SETUP.md# Настройка контроллера и радара
├── 🛠 tools/                    # Инженерные инструменты
│   ├── debug_com_port.py        # Отладка через COM (Python)
│   └── uart_engineering_setup.sh# Инженерные команды через UART
└── 📄 start.sh                  # Быстрый запуск
```

> Примечание: разделы `📊 datasets/`, `🧪 tests/`, `nginx/` отсутствуют и удалены из документации.

## 🚀 Быстрый старт

```bash
./infrastructure/scripts/configure_sensor.sh
./start.sh
```

## 📡 CAN протокол (кратко)
- 0x100 — Emergency Stop
- 0x200-0x202 — Target Data
- 0x300 — System Status
- 0x400 — Config Controller (SET/GET/SAVE/RESET)
- 0x401 — Config Radar (SET/GET/SAVE/RESET)
- 0x480/0x481 — ACK контроллера/радара

Полная спецификация: `docs/CAN_MESSAGES.md`.

## 🧰 Полезное
- Настройка через ROS: см. `services/ros2/README.md`
- Настройка/демо Node.js: `services/nodejs/stm32-config`
- Инженерный UART: `tools/uart_engineering_setup.sh`